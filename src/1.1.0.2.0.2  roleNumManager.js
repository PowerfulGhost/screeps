const logger = require("./logger")

var roleNumManager = {
    init: function () {
        logger.trace("\t\t\t\t\troleNumManager.init()", "init")
        Room.prototype.roleNumManagerRun = function () {

        }
    }
}
module.exports = roleNumManager